services: 
    frontend:
      image: ghcr.io/mina-magdy3/react-app:51188e28e0c66ce3cb62e7b5fd785ac20488e182
      networks: 
        - front_back
      ports:
       - 8080:80
      depends_on:
        - backend

    backend: 
      image: ghcr.io/mina-magdy3/dotnet:latest
      networks:
        - front_back
        - db_back
      ports:
        - "5000:5000"
      environment:
        - DB_HOST=${DB_HOST}
        - DB_PORT=${DB_PORT}
        - DB_NAME=${DB_NAME}
        - DB_USER=${DB_USER}
        - DB_PASSWORD_FILE=/run/secrets/db_password
      secrets:
        - db_password
      depends_on:
        - db

    db:
      image: mysql:8.0
      volumes:
        - db-data:/var/lib/mysql
      networks:
        - db_back
      secrets:
        - db_password
      environment:
       - MYSQL_ROOT_PASSWORD_FILE=/run/secrets/db_password
       - MYSQL_DATABASE=${DB_NAME}

volumes:
  db-data:

networks:
  front_back:
  db_back:

secrets:
  db_password:
    file: ./db_password.txt